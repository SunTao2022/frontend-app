server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
    location /stub_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }
}